# Site settings
title: Mixed Connections
description: > # this means to ignore newlines until "baseurl:"
  i miss You: Recombinant, ever changing craiglist missed connections.
baseurl: "/" # the subpath of your site, e.g. /blog/
url: "http://www.mixedconnections.us" # the base hostname & protocol for your site

# Build settings
markdown: kramdown
plugins:
  - jekyll-seo-tag
